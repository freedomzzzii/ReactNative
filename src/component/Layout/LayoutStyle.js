import { StyleSheet } from 'react-native';

const layout = StyleSheet.create({
  flex1: {
    flex: 1,
    width: '100%',
    alignItems: 'center',
  },
  logo: {
    width: '50%',
    height: 50,
    marginTop: 20,
  },
});

export default layout;
